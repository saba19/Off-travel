{% extends 'base.html.twig' %}

{% block body %}
    <h1>Package</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ package.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ package.name }}</td>
            </tr>
            <tr>
                <th>Descript</th>
                <td>{{ package.descript }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ package.price }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('packages_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('packages_edit', { 'id': package.id }) }}">Edit</a>
        </li>
        <li>
            <a href="{{ path('travel_index') }}">Back to the Main list</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
