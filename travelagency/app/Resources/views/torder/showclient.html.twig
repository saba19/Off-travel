{% extends 'base.html.twig' %}

{% block body %}
    <h1>Torder</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ torder.id }}</td>
            </tr>
            <tr>
                <th>Comments</th>
                <td>{{ torder.comments }}</td>
            </tr>
            <tr>
                <th>Phone</th>
                <td>{{ torder.phone }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('torder_index') }}">Back to the list</a>
        </li>

    </ul>
{% endblock %}
